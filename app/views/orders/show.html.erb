<h1><%= @order.food.ingredient %></h1>

<p>UOM = <%= @order.uom %></p>
<p>Quantity = <%= @order.quantity %></p>
<br>
<%= link_to "Back to list of foods", user_bag_orders_path %>
<br>
<%= link_to "Remove food", "/users/#{@user.id}/bags/#{@bag.id}/orders/#{@order.id}", method: :delete, data: { confirm: "Are you sure you want to delete #{@order.uom}?"} %>

